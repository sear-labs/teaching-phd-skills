# Credentials — never commit. Rotate, do not amend, if one lands here.
.env
*.token
*_token
*secret*
gurobi.lic

# Canvas export artifacts (may contain student names)
records/*.csv
records/*.json
!records/.gitkeep

# Student artifacts awaiting permission
exemplars/_pending/

# Student CVs and resumes. Personal data, and this repository is PUBLIC.
# Never commit these, not even redacted -- git history is permanent.
resumes/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
Thumbs.db
.DS_Store

# Editors
.vscode/
.idea/
